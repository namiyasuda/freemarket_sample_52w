-unless @brand1.nil?
  %h2.pickup__head ピックアップブランド
  %section.pickup__container
    %h3.pickup__container--text-center
      = link_to(@brand1.name+" 新着アイテム" , "")
    .pickup__container--content
      - @products5.each do |product|
        %section.item
          %figure.item__photo
            = link_to image_tag(product.images[0].name.url, alt: "実装その1", class: "lazyload"), product_path(product.id)
          .item__body
            %h3.item__body--name.font-2 
              = product.name
            .item__body--num
              .price.font-5 
                ¥
                = product.price.to_s(:delimited, delimiter: ',')
    .view-all
      = link_to("すべての商品を見る", "")